% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copreg-panel.R
\name{formula.copregpanel}
\alias{formula.copregpanel}
\title{Formula of a fitted panel copula model}
\usage{
\method{formula}{copregpanel}(x, ...)
}
\arguments{
\item{x}{A fitted \code{"copregpanel"} object.}

\item{...}{Currently unused.}
}
\value{
The two-part \code{Formula} object used to fit the model.
}
\description{
Formula of a fitted panel copula model
}
\examples{
\donttest{
set.seed(1)
N <- 30L; Time <- 6L
d <- data.frame(id = rep(seq_len(N), each = Time),
                 year = rep(seq_len(Time), times = N))
alpha <- rep(rnorm(N), each = Time)
e <- rnorm(N * Time)
d$x <- exp(rnorm(N * Time) + 0.5 * e)   # endogenous: correlated with e
d$z <- rnorm(N * Time)                  # exogenous
d$y <- alpha + 0.5 * d$x + d$z + e
fit <- CopRegPANEL(y ~ x | z, data = d, index = c("id", "year"),
                    nboots = 15, verbose = FALSE)
formula(fit)
}
}
