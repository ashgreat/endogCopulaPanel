% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copreg-panel.R
\name{confint.copregpanel}
\alias{confint.copregpanel}
\title{Confidence intervals for the coefficients of a fitted panel copula
model}
\usage{
\method{confint}{copregpanel}(object, parm, level = 0.95, type = c("normal", "percentile"), ...)
}
\arguments{
\item{object}{A fitted \code{"copregpanel"} object.}

\item{parm}{Which parameters to return intervals for; a vector of
names or indices. Defaults to all coefficients.}

\item{level}{Confidence level.}

\item{type}{\code{"normal"} (the default) for a normal-approximation
interval using the bootstrap standard error, or \code{"percentile"}
for a percentile interval taken directly from the bootstrap draws.}

\item{...}{Currently unused.}
}
\value{
A matrix with one row per parameter and the lower and upper
confidence limits in the columns.
}
\description{
Confidence intervals for the coefficients of a fitted panel copula
model
}
\examples{
\donttest{
set.seed(1)
N <- 30L; Time <- 6L
d <- data.frame(id = rep(seq_len(N), each = Time),
                 year = rep(seq_len(Time), times = N))
alpha <- rep(rnorm(N), each = Time)
e <- rnorm(N * Time)
d$x <- exp(rnorm(N * Time) + 0.5 * e)   # endogenous: correlated with e
d$z <- rnorm(N * Time)                  # exogenous
d$y <- alpha + 0.5 * d$x + d$z + e
fit <- CopRegPANEL(y ~ x | z, data = d, index = c("id", "year"),
                    nboots = 15, verbose = FALSE)
confint(fit)
confint(fit, type = "percentile")
}
}
